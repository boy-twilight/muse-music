// 浏览器本地存储枚举类型
export enum storageType {
  LOCAL = 'local',
  SESSION = 'session',
  COOkIE = 'cookie',
}

// elMessage枚举类型
export enum elMessageType {
  SUCCESS = 'success',
  ERROR = 'error',
  INFO = 'info',
  WARNING = 'warning',
}
